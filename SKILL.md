---
name: your-Design
description: >
  设计审美蒸馏器 | Design Aesthetics Distiller. 覆盖全品类风格的视觉设计分析与用户体验动线分析 Skill。
  必须使用场景：分析网站设计 / 拆解 UI/UX / 研究视觉风格 / 收录设计参考 /
  对比竞品设计 / 提炼设计规律 / 校验设计落地还原度 / 分析用户动线或转化路径。
  Also for: analyze website design, dissect UI/UX, study visual styles, collect design references,
  compare competitor designs, distill patterns, verify design-to-code fidelity, analyze user flows.
  当用户分享链接或截图且语境涉及设计/体验时，主动问"是否收录并分析"。
---

# 设计审美蒸馏器

**语言规则**：使用与用户输入相同的语言回复。用户用中文则中文输出，用户用英文则英文输出。不要根据 Skill 文件名、描述或 reference 文件推断输出语言。

## 分析哲学

每次分析遵循统一的"目标—现状—规律"三角框架，核心假设：

> 设计不是好不好看的问题，而是是否服务目标的问题。

1. **先定目标**：这个页面/产品的核心用户任务是什么？（JTBD）
2. **再看现状**：视觉和动线如何服务（或阻碍）这个目标？
3. **后提规律**：从中能提炼出什么可复用的设计原则？

分析过程先读 `references/frameworks.md` 获取适用的 UX 框架与心理学原则速查，按场景匹配使用，不全量套用。

**输出原则**：
- 每个维度描述不超过3句话，直击要点，不展开教科书式解释
- 格式塔合规度作为内部检查，输出时仅标注违规项，合规项一句总结
- 规律统一用【视觉】【体验】【落地】前缀整理
- 落地难度必须包含评级+具体原因（1句话）
- 参考 `references/seeds-public.md` 中的标杆样本格式，保持输出风格一致

---

## 一、方法论总纲

### 1.1 视觉设计分析标准（5 维 + 亮点提取）

#### 维度 1：格式塔合规度（前置检查，先跑这轮作为底层语法）

**检查清单**：

- [ ] 接近性：元素分组是否符合功能逻辑？有无距离误导让无关元素像一组？
- [ ] 相似性：同级元素（按钮、卡片、标签）视觉是否统一？异类元素是否有效差异化？
- [ ] 连续性：页面引导线（留白、分割线、排列方向）是否自然指向核心行动点？
- [ ] 闭合性：卡片裁剪、图片遮罩、截断文字是否产生正向预期？预期被满足了吗？
- [ ] 图底关系：弹窗/浮层是否清晰分离背景？关键信息是否被复杂背景淹没？
- [ ] 共同命运：联动动效是否强化模块关系？是否有动效分裂了不该分裂的信息？

**输出模板**（精简：仅标注违规项，合规项合并为一句总结）：
```yaml
格式塔合规度: 整体合规 | 存在 X 处问题:
  - [原则名]: [具体问题描述]
```

#### 维度 2：色彩系统

**检查清单**：
- [ ] 主色识别：品牌主色是什么？是否贯穿全站形成记忆锚点？
- [ ] 配色逻辑：辅色和中性色的角色是什么？主色/辅色/中性色的占比是否合理？
- [ ] 语义一致性：红=警示、绿=成功、蓝=链接 等约定是否遵循？
- [ ] 可访问性：文字与背景对比度是否足够？是否有仅依赖颜色传达信息的情况？

**输出模板**：
```yaml
色彩系统:
  主色: [色值] | 使用场景: [描述]
  辅色: [色值] | 使用场景: [描述]
  中性色: [色值范围] | 占比: [%]
  核心用色逻辑: [一句话总结该产品的配色策略]
  可访问性评估: 良好 | 存在问题: [描述]
```

#### 维度 3：排版体系

**检查清单**：
- [ ] 字体风格：衬线/无衬线？字体族选择是否与品牌调性匹配？
- [ ] 字重对比：标题/正文/标注的字重是否形成清晰的 3 级对比？
- [ ] 层级逻辑：字号跨度是否足够区分信息层级（建议 ≥2px 差异）？行高是否舒适（正文建议 1.5-1.8）？
- [ ] 信息密度：文字排版密度属于宽松/适中/紧凑？是否符合品类预期？

**输出模板**：
```yaml
排版体系:
  字体风格: [描述]
  字重对比: 标题 [字重/字号] | 正文 [字重/字号] | 标注 [字重/字号]
  层级逻辑: [描述字号体系和行高的层级关系]
  信息密度: 宽松 | 适中 | 紧凑
```

#### 维度 4：布局特征

**检查清单**：
- [ ] 栅格方式：采用了几列栅格？断点策略如何（响应式表现）？
- [ ] 留白尺度：模块间距、卡片内 padding 的统一尺度是什么？是否形成呼吸节奏？
- [ ] 构图规律：内容编排是中心对称/左对齐/卡片网格/瀑布流？
- [ ] 视觉动线：页面从上到下的信息编排是否引导用户按 F 型/Z 型自然扫视？

**输出模板**：
```yaml
布局特征:
  栅格方式: [描述]
  留白尺度: [描述间距规律]
  构图规律: [描述]
  视觉动线: F型 | Z型 | 其他: [描述]
```

#### 维度 5：组件风格

**检查清单**：
- [ ] 按钮样式：主按钮/次按钮/文字按钮的区分方式（填充、描边、颜色、投影）？圆角规则是否统一？
- [ ] 表单控件：输入框、下拉、开关的样式一致性？聚焦态是否有明显反馈？
- [ ] 卡片样式：圆角、投影、边框的规则？卡片内信息布局是否有固定模式？
- [ ] 导航组件：顶部导航/侧边导航/底部导航的形态及选中的表达方式？

**输出模板**：
```yaml
组件风格:
  按钮: 主按钮[描述] | 次按钮[描述] | 圆角[值]
  表单: [描述风格]
  卡片: 圆角[值] | 投影[描述] | 边框[描述]
  导航: [描述形态和选中态表达]
```

#### 亮点提取（后处理）

分析完 5 维后，提取 3 条最具迁移价值的视觉手法，每条包含手法描述+适用场景：

```yaml
视觉亮点:
  1. [手法描述]｜适用于: [什么场景]
  2. ...
  3. ...
```

---

### 1.2 体验动线分析标准（6 维）

> 分析基于页面视觉结构与信息逻辑推理，无需外部埋点数据。
> 基础版收录用维度 1、2、6；深度版全量 6 维。

**基础版输出模板**（用于样本收录）：
```yaml
核心任务: [用户目标，1-3个]
核心路径: [步数]步：[关键步骤串联]
动线亮点: [2-3条，每条1句话]
可复用体验规律: [1-2条，每条带适用场景]
```

**深度版**：以下 6 维全量检查并输出完整报告：

#### 维度 1：首屏注意力动线
融入F型扫视、Z型布局、视觉权重、冯·雷斯托夫。检查焦点排序、信息传达效率(3秒)、引导钩子、干扰项。

#### 维度 2：核心任务转化动线
融入JTBD、AARRR、希克定律、费茨定律。检查任务定义、路径步数、引导手段、转化触点、阻力点。

#### 维度 3：导航与信息架构动线
融入米勒定律(7±2)、雅各布定律。检查导航逻辑、层级深度(三次点击原则)、寻路友好度、选项数量。

#### 维度 4：交互反馈动线
融入多赫蒂阈值(<400ms)、特斯勒定律。检查操作反馈、状态可视化、错误引导。

#### 维度 5：流失断点与优化空间
融入峰终定律、损失厌恶。识别高概率流失节点、归因分析、优化方向。

#### 维度 6：可复用体验规律
提炼 3-4 条可迁移到同类产品的体验设计原则，标注适用场景。

**深度版输出模板**（精简：每维1-3句话核心结论）：
```yaml
首屏注意力动线: [焦点排序/传达效率/引导/干扰]
核心任务转化动线: [任务/路径/引导亮点/阻力点]
导航与信息架构: [逻辑/层级/寻路情况]
交互反馈动线: [反馈/状态/错误引导评估]
流失断点: [节点/归因/优化方向]
可复用体验原则: [3-4条]
```
```

---

### 1.3 设计-落地差异分析标准

当用户提供 UI 设计稿截图与代码 Demo 截图或链接时触发，分析视觉还原度：

**检查清单**：
- [ ] 色彩还原度：设计稿中的色值与 Demo 实际色值是否一致？色差是否在可接受范围？
- [ ] 间距还原度：margin/padding 与设计稿标注是否一致？比例关系是否保持？
- [ ] 字体还原度：字体族、字重、字号、行高是否匹配？
- [ ] 组件还原度：按钮、卡片、表单等组件的样式细节是否匹配？
- [ ] 动线还原度：设计稿规划的视觉动线与 Demo 实际表现是否一致？是否存在引导弱化、路径断裂？
- [ ] 交互还原度：hover、active、loading 等状态是否按设计实现？

**输出模板**：
```yaml
设计-落地差异分析:
  对比项: [设计稿来源] vs [Demo 来源]
  色彩还原度: 匹配 | 有偏差: [描述]
  间距还原度: 匹配 | 有偏差: [描述]
  字体还原度: 匹配 | 有偏差: [描述]
  组件还原度: 匹配 | 有偏差: [描述]
  动线还原度: 匹配 | 有偏差: [描述]
  交互还原度: 匹配 | 有偏差: [描述]
  还原度总评: [%] | 关键问题: [列出]
  改进建议: [列出具体操作建议]
```

---

## 二、指令全集与调度

### 2.1 指令路由表

| 触发词/用户意图 | 调度模块 | 分析深度 | 输出产物 | 写入位置 |
|-----------------|---------|---------|---------|---------|
| "收录并分析"/分享链接+设计语境 | 样本收录与分析 | 基础版 | 设计样本卡片（5维视觉+基础动线）| `user/samples/` |
| "分析动线"/"拆解转化路径" | 动线分析 | 基础版 | 动线分析追加到样本卡片 | `user/samples/` |
| "深度拆解"/"详细分析体验" | 动线分析 | 深度版 | 独立动线分析卡片 | `user/journeys/` |
| "对比这两个"/"横向对比" | 检索与蒸馏 | — | 差异对比表 | 直接输出 |
| "总结XX品类的规律"/"蒸馏" | 规律蒸馏 | — | 品类规律卡片 | `user/patterns/` |
| "对比UI稿和Demo"/"校验还原度" | 落地差异分析 | — | 差异分析报告 | `user/samples/` |
| "设计一个XX"/"生成XX方案"/"参考XX风格设计" | 设计指导 | — | 视觉方案+动线建议 | 直接输出 |
| "查看我的XX库"/"有哪些样本" | 检索浏览 | — | 统计数据/列表 | 直接输出 |

### 2.2 混合模式执行逻辑

```
用户输入 → 匹配 description 中的触发域？
              │
        否 ──┼── 是 → 识别指令类型
                     │
            明确匹配路由表 → 执行对应模块
                     │
            无明确指令但有链接/截图+设计语境 →
                    主动问："是否对这个网站进行收录和分析？"
                    是 → 执行样本收录（基础版）
                    否 → 不执行
```

### 2.3 分析深度自动判定

- **基础版**（默认）：收录样本时自动执行，视觉 5 维 + 动线维度 1、2、6（首屏注意力 + 核心任务 + 可复用规律），输出精简结论
- **深度版**（用户明确指令触发）：动线 6 维全量拆解，输出完整报告，生成独立 `E-USER-xxx` 动线卡片
- 同品类样本积累 ≥3 个时，分析完成后主动问"是否蒸馏该品类的设计规律？"

---

## 三、模块执行细则

### 3.1 样本收录与分析模块

**触发**：用户说"收录并分析这个网站" 或 分享链接在混合模式下确认收录

**执行步骤**：

1. **获取内容**：使用 WebFetch 获取网页，或接收用户提供的截图作为分析素材

2. **加载框架**：读取 `references/frameworks.md`，按收录场景匹配适用的框架卡片

3. **执行分析**（基础版）：
   - 内部跑格式塔合规度检查（6 子原则），输出时仅标注违规项
   - 按 1.1 视觉 5 维逐一分析，每个维度输出 1-3 句话核心结论
   - 按 1.2 基础版模板输出动线结论
   - 规律汇总统一用【视觉】【体验】【落地】前缀

4. **生成卡片**：按 4.1 设计样本卡片模板组装内容，自动生成样本 ID（`S-USER-YYYY-MMDD-序号`）

5. **写入文件**：
   ```
   user/samples/S-USER-YYYY-MMDD-[序号].md
   ```

6. **归档完成**：告知用户收录结果，列出卡片中的 3-5 个视觉亮点和 1-2 条可复用规律

**同品类积累检查**：收录完成后，检查 `user/samples/` 中同品类样本数量，≥3 个时询问"该品类已有 X 个样本，是否需要蒸馏该品类的设计规律？"

### 3.2 动线与体验分析模块

**触发**：用户说"分析动线""拆解转化路径""深度拆解"

**基础版执行步骤**：

1. 定位目标样本（如果是新分析，先按 3.1 执行收录）
2. 按 1.2 维度 1、2、6 执行分析
3. 将结果写入已存在的样本卡片的"用户动线基础分析"区块

**深度版执行步骤**：

1. 按 1.2 全量 6 维执行分析
2. 代入三类用户视角（新访客/目标用户/漫游用户）分别推演动线
3. 每维对照检查清单自问，填写输出模板
4. 生成独立的动线分析卡片（`E-USER-YYYY-MMDD-序号`）
5. 写入文件：
   ```
   user/journeys/E-USER-YYYY-MMDD-[序号].md
   ```

### 3.3 检索与规律蒸馏模块

**触发**："总结XX品类的规律""蒸馏XX规律""对比XX和XX"

**蒸馏执行步骤**：

1. 检索 `user/samples/` + `references/seeds-public.md` 中同品类/同风格的样本
2. 交叉对比各样本的分析结论
3. 提炼共性规律，按"视觉规律 + 动线规律"双维度输出
4. 标注置信度（高/中/低，取决于样本数量和样本质量）
5. 写入文件：
   ```
   user/patterns/[品类]-[类型]-patterns.md
   ```

**对比执行步骤**：

1. 读取两个指定样本的完整分析
2. 按维度生成差异对比表
3. 标注各维度的优劣势及适用场景

### 3.4 设计-落地差异分析模块

**触发**："对比UI稿和Demo""校验还原度""比对设计稿和实现"

**执行步骤**：

1. 接收用户提供的 UI 设计稿截图和 Demo 截图（或链接）
2. 按 1.3 的 6 项检查清单逐项对比
3. 输出差异分析报告（参见 1.3 输出模板）
4. 给出还原建议，按优先级排序
5. 写入文件（可选，询问用户是否需要归档）：
   ```
   user/samples/GAP-YYYY-MMDD-[序号].md
   ```

### 3.5 设计指导应用模块

**触发**："设计一个XX""参考XX风格设计XX""怎么做XX品类页面"

**执行步骤**：

1. 明确设计目标（品类、受众、核心任务）
2. 检索 `user/patterns/` 和 `references/seeds-public.md` 中的相关规律与样本
3. 按品类匹配标准动线建议
4. 输出结构：

```markdown
## 设计方案：[标题]

### 视觉方案
- 色彩策略: [基于蒸馏规律的建议]
- 排版建议: [字体、字重、层级]
- 布局方案: [栅格、留白、构图]
- 组件风格: [关键组件的设计建议]
- 参考样本: [列出相关的标杆样本]

### 动线与交互方案
- 首屏信息排布: [基于品类标准动线的建议]
- 核心转化路径: [路径设计建议]
- 导航架构: [导航方案建议]
- 关键交互: [反馈和状态设计建议]
```

---

## 四、数据规范速查

### 4.1 设计样本卡片

```yaml
样本ID: S-USER-YYYY-MMDD-序号 | S-PUB-品类-序号
来源类型: 用户新增 | 公版内置
=== 基础信息 ===
网站/作品名称:
官方链接:
所属品类: 一级品类 / 二级细分
所属地区: 地区 / 风格流派
核心风格标签: [3-5个标签]
目标受众:
落地难度预估: 低 | 中 | 高（原因：1句话说明）
=== 设计系统拆解 ===
色彩系统: [主色/辅色/中性色 + 核心用色逻辑]
排版体系: [字体/字重/层级/密度]
布局特征: [栅格/留白/构图/动线]
组件风格: [按钮/表单/卡片/导航]
视觉亮点: [3条，每条1-2句]
=== 用户动线基础分析 ===
核心任务: [定义]
核心路径: [步骤简述]
动线亮点: [2-3条]
可复用体验规律: [1-2条]
=== 可复用规律汇总 ===
1. 【视觉】[规律] ｜适用：[场景]
2. 【体验】[规律] ｜适用：[场景]
3. 【落地】[规律] ｜适用：[场景]
```

### 4.2 动线深度分析卡片

```yaml
样本ID: E-USER-YYYY-MMDD-序号
关联样本ID: [S-xxx]
来源类型: 用户新增
分析时间:
=== 基础信息 ===
产品/网站名称:
所属品类:
分析类型: 首页全动线 | 核心任务全流程 | 特定页面
=== 完整动线拆解 ===
1. 首屏注意力动线: [焦点排序/效率/引导/干扰]
2. 核心任务转化动线: [任务/路径/引导/阻力]
3. 导航与信息架构动线: [逻辑/层级/寻路]
4. 交互反馈动线: [操作反馈/状态/错误引导]
5. 流失断点: [节点/归因/优化方向]
6. 可复用体验原则: [3-4条]
=== 用户标注区 ===
个人评分:
备注:
是否标杆: 是 | 否
```

### 4.3 规律蒸馏卡片

参考 `references/seeds-public.md` 中标杆样本的格式：
```yaml
规律ID: P-USER-YYYY-MMDD-序号
品类: [一级/二级]
=== 规律 ===
1. 【视觉】[规律] ｜置信度: 高/中/低 ｜适用: [场景]
2. 【体验】[规律] ｜置信度: 高/中/低 ｜适用: [场景]
3. 【落地】[规律] ｜置信度: 高/中/低 ｜适用: [场景]
=== 避坑提示 ===
[该品类常见的体验断点]
```

---

## 五、维护指引

### 5.1 版本更新

- 公版更新时，仅替换 `SKILL.md` 和 `references/` 目录，`user/` 目录完全保留
- 新版本方法论如新增分析维度，旧样本卡片缺失该字段属正常，不影响使用
- 更新后首次使用时，提示"公版内核已更新至 vX.X，新增了 [功能描述]"

### 5.2 用户数据迁移

- `user/` 目录内容永不被自动修改
- 样本 ID 使用时间戳序号，确保唯一且可追溯
- 规律蒸馏卡片通过参考样本 ID 列表保持与原始样本的关联
- `references/seeds-public.md` 随公版更新替换，用户无需迁移

---

## 参考文件

- `references/frameworks.md`：实战 UX 框架与心理学原则速查，分析时按场景按需读取
- `references/seeds-public.md`：公版内置种子库，按品类×地区×风格组织标杆样本
- `user/`：用户私有数据库，Skill 更新不覆盖
